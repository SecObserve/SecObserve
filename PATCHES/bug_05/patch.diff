--- a/backend/application/notifications/api/views.py
+++ b/backend/application/notifications/api/views.py
@@ -19,11 +19,8 @@ from application.notifications.api.serializers import (
     NotificationSerializer,
 )
 from application.notifications.models import Notification, Notification_Viewed
-from application.notifications.queries.notification import (
-    get_notification_by_id,
-    get_notifications,
-)
+from application.notifications.queries.notification import get_notifications
 from application.notifications.services.notification import bulk_mark_as_viewed
 
 
 class NotificationViewSet(GenericViewSet, DestroyModelMixin, ListModelMixin, RetrieveModelMixin):
@@ -64,13 +61,12 @@ class NotificationViewSet(GenericViewSet, DestroyModelMixin, ListModelMixin, Ret
     )
     @action(detail=True, methods=["post"])
     def mark_as_viewed(self, request: Request, pk: int) -> Response:
-        if not get_notification_by_id(pk):
-            return Response(status=HTTP_404_NOT_FOUND)
+        notification = self.get_object()
 
         user = request.user if isinstance(request.user, User) else None
 
         Notification_Viewed.objects.update_or_create(
-            notification_id=pk,
+            notification_id=notification.pk,
             user=user,
         )
         return Response(status=HTTP_204_NO_CONTENT)
--- a/backend/unittests/authorization/api/test_authorization_notifications.py
+++ b/backend/unittests/authorization/api/test_authorization_notifications.py
@@ -33,6 +33,18 @@ class TestAuthorizationNotifications(TestAuthorizationBase):
         self._test_api(APITest("db_internal_write", "get", "/api/notifications/1/", None, 404, None))
 
         self._test_api(APITest("db_internal_write", "get", "/api/notifications/3/", None, 200, None))
+
+        # Notification 1 is outside db_internal_write's scope (filtered by get_notifications);
+        # mark_as_viewed must return 404, not 204, so it cannot be used as an existence oracle.
+        self._test_api(
+            APITest("db_internal_write", "post", "/api/notifications/1/mark_as_viewed/", None, 404, None)
+        )
+
+        # An in-scope notification can still be marked as viewed.
+        self._test_api(
+            APITest("db_internal_write", "post", "/api/notifications/3/mark_as_viewed/", None, 204, None)
+        )
 
         post_data = {"notifications": [1, 3, 5]}
         expected_data = "{'message': 'Some notifications do not exist'}"
